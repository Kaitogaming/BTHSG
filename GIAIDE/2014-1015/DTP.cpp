#include<bits/stdc++.h>
#define ll long long
ll n,x1,x2,y1,y2;
using namespace std;
bool check[10000][10000];
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    freopen("DTP.INP","r",stdin);
    freopen("STP.OUT","w",stdout);
    memset(check,0,sizeof(check));
    cin>>n;
    for(i=0;i<n;i++)
    {
        cin>>x1>>y1>>x2>>y2;
        for(i=x1;i<=x2;i++)
        {

        }
    }
}
